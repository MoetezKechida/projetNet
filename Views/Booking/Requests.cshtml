@model IEnumerable<projetNet.Models.Booking>
@{
    ViewData["Title"] = "Booking Requests";
}

<div class="container mt-4">
    <h2>Booking Requests</h2>
    <p class="text-muted">View all booking requests from buyers interested in your vehicles.</p>

    @if (!Model.Any())
    {
        <div class="alert alert-info">
            No booking requests yet.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
                    <tr>
                        <th>Booking ID</th>
                        <th>Vehicle ID</th>
                        <th>Type</th>
                        <th>Buyer ID</th>
                        <th>Start Date</th>
                        <th>End Date</th>
                        <th>Total Amount</th>
                        <th>Date Created</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var booking in Model)
                    {
                        <tr>
                            <td>@booking.Id.ToString().Substring(0, 8)...</td>
                            <td>@booking.VehicleId.ToString().Substring(0, 8)...</td>
                            <td><span class="badge bg-@(booking.BookingType == "Buy" ? "primary" : "success")">@booking.BookingType</span></td>
                            <td>@booking.BuyerId.Substring(0, 8)...</td>
                            <td>@(booking.StartDate.Year > 1900 ? booking.StartDate.ToString("dd/MM/yyyy") : "N/A")</td>
                            <td>@(booking.EndDate.Year > 1900 ? booking.EndDate.ToString("dd/MM/yyyy") : "N/A")</td>
                            <td><span class="badge bg-success">@string.Format("{0:F2}", booking.TotalAmount) €</span></td>
                            <td>Just now</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

    <div class="mt-3">
        <a asp-controller="Vendeur" asp-action="Index" class="btn btn-secondary">Back to Dashboard</a>
    </div>
</div>

<style>
    .table-responsive {
        margin-top: 20px;
    }
    
    .table td {
        vertical-align: middle;
    }
</style>
