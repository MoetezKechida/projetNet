@{
    ViewData["Title"] = "Vehicle Accepted";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm border-success">
                <div class="card-body text-center">
                    <div class="mb-4">
                        <span class="text-success display-4">✔</span>
                    </div>

                    <h2 class="text-success mb-3">Vehicle Accepted</h2>

                    <p class="text-muted">
                        The vehicle has been successfully reviewed and accepted.
                        It is now visible to users according to the platform rules.
                    </p>

                    <div class="d-flex justify-content-center gap-3 mt-4">
                        <a asp-action="Index" class="btn btn-primary">
                            Back to Vehicles
                        </a>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>